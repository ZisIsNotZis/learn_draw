# 15 — L7 withdrawal A: redraw the assembled figure with the image closed

Status: ready-for-agent (blocked)
Blocked by: 13-assembly

## Issue

The curriculum promises the withdrawal to reference-free drawing but never trains it (SA1 finding 7).
Rungs 08–13 always measure against the reference; the skill of *recalling* canon fractions without
measuring — the entire content of "drawing without a reference" — has no practice slot.

## Scope

Reproduce the L6 assembled figure **from its own spec, with the reference closed**. Mechanically:
the spec from 13 is available (it is the compressed, transferable record of the drawing), but the
image is not. Every value the spec lacks must be re-derived from canons (`vocabulary.md`) or stated
as a taste choice with provenance. This measures **recall**, not tracing.

## Acceptance

- [ ] image.jpg unavailable for the whole exercise (checked at start; any use = restart)
- [ ] fresh-eyes review vs the L6 render: reads as the same subject and style
- [ ] every value not derivable from the spec is stated with provenance (canon / taste / new
      measurement) — no silent eyeballing
- [ ] canon values re-derived from memory that differ from the measured ones are recorded as deltas
      (that delta list is the learning signal for rung 16)
- [ ] diagnostics clean; iteration log; evidence committed

## Comments

- 2026-09-14 agent(pi): created from SA1 finding 7. The withdrawal is the point of the whole project;
  it gets real rungs with real acceptance, not a comment.
