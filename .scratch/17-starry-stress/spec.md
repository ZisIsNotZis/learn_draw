# 17 — L9 stress test: a Starry-Night-class scene from intent alone

Status: ready-for-agent (blocked)
Blocked by: 16-withdrawal-b

## Issue

The end goal's complexity stress test: a complex, non-portrait, intent-only scene. SA3's probe
(`.scratch/14-abstraction-research/work/starry/`, evidence `evidence/starry/`) already proved the
*feasibility* — a credible Starry Night evocation from ~10 hand numbers + parameterized nodes, no
reference anywhere — and delivered the family list this rung demands. This rung makes it a real
drawing with real acceptance, not a probe.

## What this rung may demand (SA3's measured list — all dated NOW, none speculative)

- Generator/vocabulary families: `swirl` (nested spiral ribbons), `flow` (direction-field ribbons —
  the biggest gap, also wanted by hair/water), `burst` + `glow` (stars/moon: halo/rays/core),
  `flame` (cypress: scalloped taper + wisps), `village` (distribute-along a curve, tangent frames),
  `hill` (stacked far-light→near-dark bands), `stars` (jitter-grid scatter)
- Relations to promote to first-class: `flow`, `distribute-along`, `jitter-grid` (each currently
  lives once, inside one family, ad hoc)
- The `generator:` bridge: a spec-level node that declares "compile me as back-end generator X with
  these resolved params and report true bounds" — the declarative-family mechanism (14 D3)

## Acceptance

- [ ] a new scene (NOT the SA3 probe's spec — same complexity class, new subject matter or
      recomposed) from intent alone, deterministic seeds
- [ ] ≤ ~10 hand numbers for the whole scene; everything else parameters/relations
- [ ] families in `vocabulary.md` the same session they land (docs-first)
- [ ] fresh-context reviewer: recognizes the scene type unprompted; bundle via `draw check`
- [ ] honest-limits section in the ticket: what reads as art vs as boxes, recorded

## Comments

- 2026-09-14 agent(pi): created from SA3's verdict. The probe stays in 14 as evidence; this rung is
  the demand date for everything it found.
