# 01 — warmup-duck: full loop end-to-end (T1 line art + T2 flat color)

Status: done

## Goal
Shake out the compare/diff/ref/log toolkit and the drawing loop on a simple subject before the image.jpg portrait.

## Reference
`.scratch/01-warmup-duck/ref/src-duck.svg` — generated by a fresh-context delegate subagent (provenance only; never read as source during drawing). Rendered to `ref/duck.png` (800×800); all drawing done against the PNG + derived refs (lineart/palette).

## Result
`work/v7.svg` — readable, fully commented, semantic layer structure (`scene/duck` groups, ids per shape, intent comments). Final: edge-F1 ≈ 0.55, color_dist ≈ 9.6. Evidence per iteration in `evidence/`, reflections in `log.md`.

## Acceptance
- [x] line art: silhouette + all 15 inventory shapes match (diff mostly white/fringe)
- [x] flat color: palette sampled from ref, all regions filled, water decoded
- [x] every iteration committed, logged with metrics + written reflection
- [x] fresh-eyes critic review ran; findings triaged (3 actionable → v7)
- [x] 7 transferable principles distilled to docs/drawing/principles.md

## Knowhow learned
Promoted to `scripts/draw.py measure` (point color sample, Hough circles, row/col transition scans) and principles P1–P7. Tool notes: chrome-headless-shell renderer + file:// abspath bug (00-tooling ticket).

## Comments
- 2026-09-10 agent(pi): 7 iterations (v1-v7). Key pivots: measured geometry (v3, F1 0.30→0.49), water semantics decoded via pixel scans (v4/v5, color_dist 85→9.5), critic-driven junction fixes (v7). Reference generated by delegate subagent to avoid self-copying bias.
